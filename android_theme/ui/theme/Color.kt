package com.example.archibald.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Archibald Visual Identity - Color Palette
 * Translated precisely from the premium holographic web interface design.
 */

// Core Dark Cyber Canvas Backgrounds
val CosmicDark = Color(0xFF020409)          // Main deep-space background
val GlassPanelBg = Color(0x0DFFFFFF)        // 5% white backdrop blur panel

// Cosmic Glow Energetic Accents
val CyanGlow = Color(0xFF22D3EE)           // Core cyber cyan glow
val BlueGlow = Color(0xFF0EA5E9)           // Auxiliary deep cyber-blue
val EmeraldGlow = Color(0xFF34D399)        // Active listener green
val RoseGlow = Color(0xFFF43F5E)           // Matrix diagnostic rose-red

// Ultra-fine Laser Borders and Reflections
val LaserBorderCyan = Color(0x4D22D3EE)    // 30% opacity cyan border
val LaserBorderWhite = Color(0x19FFFFFF)   // 10% opacity white boundary
val WaveParticleWhite = Color(0xFFFFFFFF)  // Full intensity white center highlights

// Specific Chat Bubble Color Tokens (Translucent & highly glassmorphic)
val UserChatBg = Color(0x660A192F)         // 40% translucent deep-blue core
val ArchiChatBg = Color(0x66051121)        // 40% translucent cyan-tinted core

// High-fidelity Text and Contrast States
val TextWhite = Color(0xFFFFFFFF)          // High contrast layout content
val TextGlassMuted = Color(0xCCFFFFFF)     // 80% opacity text for secondary indicators
val TextGlassMutedDim = Color(0x66FFFFFF)  // 40% opacity text for icons/inactive tabs
val TimestampMuted = Color(0x4DFFFFFF)     // 30% opacity for timestamps
val ActiveDotGlowing = Color(0xFF00E5FF)   // Glowing indicator matrix center
